<UserControl x:Class="SkyboundLauncher.ForgotPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#0f0f1e"
             Foreground="#ffffff">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30,20" Orientation="Vertical" VerticalAlignment="Top">
            <!-- Main title -->
            <TextBlock Text="Want to change your account password?" FontSize="18" Foreground="#00d9ff" FontWeight="Bold" Margin="0,0,0,20" TextAlignment="Center"/>

            <!-- Username field -->
            <TextBlock Text="Username:" FontSize="12" Foreground="#cccccc" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameTextBox" 
                     Background="#1a1a2e" 
                     Foreground="#ffffff" 
                     BorderBrush="#333333" 
                     BorderThickness="1"
                     Padding="10,8"
                     Margin="0,0,0,15"
                     FontSize="12"/>

            <!-- Email field -->
            <TextBlock Text="Email:" FontSize="12" Foreground="#cccccc" Margin="0,0,0,5"/>
            <TextBox x:Name="EmailTextBox" 
                     Background="#1a1a2e" 
                     Foreground="#ffffff" 
                     BorderBrush="#333333" 
                     BorderThickness="1"
                     Padding="10,8"
                     Margin="0,0,0,15"
                     FontSize="12"/>

            <!-- Discord Username field -->
            <TextBlock Text="Discord Username (for password change notification):" FontSize="12" Foreground="#cccccc" Margin="0,0,0,5"/>
            <TextBox x:Name="DiscordUsernameTextBox" 
                     Background="#1a1a2e" 
                     Foreground="#ffffff" 
                     BorderBrush="#333333" 
                     BorderThickness="1"
                     Padding="10,8"
                     Margin="0,0,0,25"
                     FontSize="12"/>

            <!-- Submit button -->
            <Button x:Name="SubmitButton"
                    Content="Submit"
                    Click="SubmitButton_Click"
                    Background="#00d9ff"
                    Foreground="#0f0f1e"
                    Padding="20,8"
                    FontSize="12"
                    FontWeight="Bold"
                    Cursor="Hand"
                    BorderThickness="0"
                    Width="120"
                    HorizontalAlignment="Center">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Status message -->
            <TextBlock x:Name="StatusMessage" 
                       Text="" 
                       FontSize="11" 
                       Foreground="#cccccc" 
                       Margin="0,15,0,0" 
                       TextAlignment="Center"
                       TextWrapping="Wrap"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
