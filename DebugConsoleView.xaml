<UserControl x:Class="SkyboundLauncher.DebugConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#0f0f1e"
             Foreground="#ffffff">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Debug Output -->
        <Border Grid.Row="0" Background="#1a1a2e" BorderThickness="0,0,0,1" BorderBrush="#333333" Margin="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="DebugTextBox" 
                         Background="#0f0f1e"
                         Foreground="#00d9ff"
                         BorderThickness="0"
                         Padding="10"
                         IsReadOnly="True"
                         FontFamily="Consolas"
                         FontSize="11"
                         TextWrapping="Wrap"
                         VerticalAlignment="Stretch"/>
            </ScrollViewer>
        </Border>

        <!-- Bottom toolbar -->
        <Border Grid.Row="1" Background="#1a1a2e99" BorderThickness="0,1,0,0" BorderBrush="#333333" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Clear" Click="ClearButton_Click"
                       Background="#404040" Foreground="#ffffff" 
                       Padding="10,6" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button x:Name="AutoScrollButton"
                       Content="Auto-scroll: ON"
                       Click="AutoScrollButton_Click"
                       Background="#00d9ff" Foreground="#0f0f1e" 
                       Padding="10,6" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
